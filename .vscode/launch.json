{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/reactjs-demo/package.json",
                "localRoot": "${workspaceFolder}/reactjs-demo"
            }
        }
    ]
}